import React from 'react';

const Home = () => (
  <>
    <h1>Home Page</h1>
    <p> I should create a collage for all of the buttons and have them display the collage on hovering the buttons in the sidebar </p>
  </>
)

export default Home;
